# Session 1 강의 노트 — 데이터 수집·정제 (주제1: 우크라이나)

> 강사가 화면을 공유하며 슬라이드(`session1.html`)와 `session1.ipynb` 를 한 장/한 셀씩 따라가는 스크립트. 슬라이드 23장과 1:1로 정렬돼 있다.
>
> **운영 원칙**
> - 설명은 30초 안에 끊는다. 길어지면 학생이 "아는 척"으로 도망간다.
> - 매 셀 실행 직후 **"지금 뭐가 나왔지?"** 한 번 묻는다. 답 못 하면 안 넘어간다.
> - 개념보다 **"이 줄이 없으면 뭐가 깨지나"** 를 먼저 보여준다.
> - 셀은 위에서 아래로 하나씩. `# TODO` 채우고 `# CHECK` 에서 ✅ 뜨면 다음으로.

---

## 슬라이드 01 — 외교 언어, 숫자로 읽다 (타이틀)

**한 줄 요약**: 오늘의 주제 선언 — UN·한국·중국·프랑스가 같은 사건에 쓴 다른 단어를 숫자로 읽는다.

**설명 포인트**
- 부제를 그대로 읽는다: "UN · 한국 · 중국 · 프랑스 — 같은 사건, 다른 단어."
- Session 01의 정체성: **데이터 수집과 정제**, 주제는 **우크라이나**.

(전환 슬라이드 — 길게 끌지 말고 바로 훅으로 넘어간다.)

---

## 슬라이드 02 — 한 문장: 같은 전쟁, 다른 언어 (훅)

**한 줄 요약**: 오프닝 훅 — 같은 침공, 정반대의 외교 단어.

**설명 포인트 (무엇을/왜)**
- "2022년 2월 24일, 러시아가 우크라이나를 침공했다. 같은 날, 전 세계 외교부가 성명을 냈다."
- **한국은 'condemn(규탄)'**, **중국은 'all parties(모든 당사자)'**. 같은 사건, 다른 단어.
- 메시지: "오늘부터 우리는 이 차이를 **숫자**로 잡는다." 이 한 문장이 6주 전체의 출발점이다.

**검증 질문**: "'규탄'과 '모든 당사자'는 무엇이 다르지? 둘 다 사실을 말하는데 왜 느낌이 다를까?"

---

## 슬라이드 03 — 오늘의 흐름: 6 Step (아젠다)

**한 줄 요약**: 오늘 90분을 6개 Step으로 미리 펼쳐 보여준다.

**설명 포인트**
- Step 0 환경 설정 → Step 1 존재 확인 → Step 2 로드 → Step 3 정제 → Step 4 필터(우크라 157건) → Step 5~6 침묵 지도 + 저장.
- **오늘의 두 봉우리: Step 3 정제 · Step 5 침묵.** 여기에 시간을 더 쓴다고 미리 예고한다.
- 운영 방식 강조: 셀을 위에서 아래로 하나씩, `# TODO` 채우고 `# CHECK` 에서 ✅ 뜨면 다음으로.

(가볍게 훑고 넘어가는 지도용 슬라이드.)

---

## 슬라이드 04 — 프로젝트 전체 지도: 9개 분석 차원

**한 줄 요약**: 6주 프로젝트가 만들 **9개 분석 차원**과 Hybrid 방법론을 한눈에.

**설명 포인트**
- **핵심 6차원**: 직설성 · 동사강도 · 주어패턴 · 상호성 · 완곡어 · 침묵지도.
- **확장 3차원(v2)**: 완곡명명(Event Naming) · 귀속(Blame) · 대상별 태도(Targeted Sentiment).
- **Hybrid 방법론**: 계산은 코드, 해석은 LLM(Claude), 판단은 인간. 이 세 줄이 프로젝트 철학이다.
- 하단 띠: s1 수집·정제 → s2 구조 → s3 의미 → s4 검증·시각화 → s5~s6 가자 → 과제 AI.

**검증 질문**: "오늘(s1)은 9개 중 어느 차원의 원재료를 만들까?" → (6 침묵지도)

---

## 슬라이드 05 — 9개 차원, 어느 세션에서 배우나

**한 줄 요약**: 9개 차원이 세션별로 어떻게 분배되는지 표로 확정.

**설명 포인트**
- s1(오늘): 데이터 + **6 침묵지도** 원재료.
- s2: 1 직설성 · 2 동사강도 · 3 주어 + **8 귀속**.
- s3: 4 상호성 · 5 완곡어(부정처리) + **9 대상별 태도**.
- s4: 검증 · 시각화 + **7 완곡명명(원어 검증)**.
- s5~s6: 가자에 9개 전부 재적용 + 시계열.
- 한 줄 요약으로 박기: **귀속=s2, 대상별태도+부정처리=s3, 완곡명명+원어검증=s4.**

---

## 슬라이드 06 — 오늘(Session 1)의 목표

**한 줄 요약**: 오늘 끝나면 할 수 있어야 하는 4가지.

**설명 포인트**
1. 원천데이터가 **진짜 존재하는지** 두 눈으로 확인 — 분석의 첫 윤리.
2. 데이터를 불러와 **스키마(구조)** 이해.
3. 공개 데이터의 **현실(지저분함)** 을 보고 무엇을 버릴지 정함 = 정제.
4. 우크라이나 작업셋을 만들고 **침묵 지도**를 처음 그림.

**검증 질문**: "이 중 어느 게 제일 어려울 것 같아?" → 보통 3번(정제). 그래서 거기 시간을 쓴다고 연결.

---

## 슬라이드 07 — Step 0: 환경 설정, 폴더 자동 탐색 (코드)

**한 줄 요약**: spaCy·plotly 설치 + `find_project()` 로 백업 코퍼스 폴더를 자동으로 찾는다.

**설명 포인트**
- `find_project()` 가 여러 후보 경로(`.`, `program4`, `..`, `/content/program4`)를 돌며 `data/backup/corpus_clean.json` 이 있는 폴더를 작업 경로로 잡는다.
- 핵심: Colab이든 로컬이든 **같은 코드가 돈다.**

**시연 포인트**
- 셀 실행 → `✅ 프로젝트 경로: ...` 가 뜨는 걸 보여준다.
- Colab이면 좌측 파일창에 program4 업로드 또는 `!git clone`, 로컬이면 자동.

**검증 질문**: "`✅ 프로젝트 경로:` 떴어요? 안 떴으면 손 드세요." → 여기서 한 명도 안 막히게 한다.

---

## 슬라이드 08 — Step 1: 왜 '존재 확인'이 1번인가 🔍

**한 줄 요약**: 분석 전 데이터 존재를 확인하는 건 첫 번째 윤리다.

**설명 포인트 (무엇을/왜)**
- 분석가가 제일 많이 하는 실수: **"데이터가 있겠지"** 하고 분석부터 짠다.
- 파일이 없거나·비었거나·깨졌으면 → **그 위에 쌓은 모든 분석은 거짓말**이 된다.
- **존재 → 개수 → 내용** 3단 확인을 첫날에 습관으로 박는다.

**검증 질문**: "왜 분석 코드가 아니라 존재 확인부터일까?" → (틀린 토대 위 분석은 전부 무효)

---

## 슬라이드 09 — Step 1: 코드, 파일 존재 + 크기 확인

**한 줄 요약**: 필요한 4개 파일을 돌며 존재 여부와 bytes 크기를 찍는다.

**설명 포인트**
- `need` 리스트(corpus_clean.json/csv, coverage_clean.csv, manifest.json)를 순회.
- `os.path.exists` 로 존재, `os.path.getsize` 로 크기. ✅/❌ + bytes 출력.

**시연 포인트**
- "존재만 보면 안 된다 — **0 bytes면 파일은 있어도 빈 파일**." 그래서 크기까지 찍는다.

**검증 질문**: "✅ 떴는데 size가 0이면 어떻게 해야 하지?" → (있다고 착각하면 안 됨, 빈 파일 취급)

---

## 슬라이드 10 — 우리가 실제로 모은 것 (manifest)

**한 줄 요약**: 4개 정부·기구를 직접 스크래핑해 **raw 371 → 정제 354건**.

**설명 포인트**
- 솔직하게: "너희가 모은 건 아니다. 하지만 **스크래핑 코드는 `scrapers/` 에 다 있다.**"
- 라이브가 막히면 백업(JSON/CSV)으로 돌아간다 — 그래서 둘 다 둔다.
- 규모: UN·한국·중국·프랑스, **우크라이나 157 · 가자 · AI · 30/30 사건.**

**검증 질문**: "총 354건. 중국이 왜 건수가 많을까?" → (매일 정례 브리핑) Step 3 정제의 복선으로 연결.

---

## 슬라이드 11 — Step 2: 로드, JSON → 표(DataFrame) (코드)

**한 줄 요약**: `corpus_clean.json` 을 읽어 pandas DataFrame으로 올린다.

**설명 포인트**
- JSON은 **딕셔너리들의 리스트**. `pd.DataFrame(docs)` 에 올리면 딕셔너리의 키가 **열**, 한 건이 **행**.
- `df.shape` 로 (행, 열) 확인, `df.head(3)` 으로 앞 3건을 표로.

**시연 포인트**
- `df.head(3)` 실행해 엑셀처럼 보이는 표 모양을 보여준다. "이제 데이터를 표로 다룬다."

---

## 슬라이드 12 — 스키마: 어떤 열이 있나

**한 줄 요약**: DataFrame의 각 열이 무슨 뜻인지 표로 읽는다.

**설명 포인트**
- `source` = UN/KR/CN/FR, `topic` = ukraine/gaza/ai_governance.
- `event_id · event_name · date` = 어떤 사건·발표일, `title · url` = 제목과 원문 링크(검증 때 클릭).
- **`text` = 성명 본문 = 우리의 분석 대상** (가장 강조).
- `collected_via=live_scrape` = 실제로 우리가 긁어옴.

**검증 질문**: "9개 차원 분석은 결국 어느 열을 보는 걸까?" → (`text`)

---

## 슬라이드 13 — 실습: 소스별 몇 건? (value_counts)

**한 줄 요약**: `value_counts()` 로 소스별 건수를 세는 첫 TODO 실습.

**설명 포인트**
- `value_counts()` 는 각 값이 **몇 번 나오는지** 세어 큰 순서로 돌려준다.

**시연 포인트 / TODO**
- 빈칸 `df["source"].____()` 를 학생이 직접 친다. 막히면 정답 셀(`value_counts()`).

**검증 질문 (복선)**: "프랑스가 건수는 1위인데 **평균 글자수는 가장 짧다.** 왜일까?" → Step 3로 이어지는 길이 편향 복선.

---

## 슬라이드 14 — Step 3: 정제, 공개 데이터의 현실

**한 줄 요약**: 공개 데이터는 지저분하다 — 중국 브리핑이 대표 사례.

**설명 포인트 (이 프로젝트에서 제일 중요한 30초)**
- 스키마 다시 짚기: source·topic·event·date·title·url·**text**·collected_via.
- "중국 외교부는 사건마다 성명을 안 낸다 — **매일 정례 브리핑** 한 번에 우크라이나·가자·양자관계·기자질문을 다 답한다."
- 그래서 원본 한 건이 **1만 8천 자**, 여러 주제가 한 덩어리로 섞인다.

**검증 질문**: "이걸 그대로 분석하면 무슨 문제가 생길까?" → (다른 주제 텍스트가 우크라 분석을 오염)

---

## 슬라이드 15 — Step 3: 코드, raw vs clean 평균 글자수

**한 줄 요약**: 소스별 평균 글자수를 재서 정제 효과와 길이 편향을 손으로 확인.

**설명 포인트**
- `avglen(data, src)` = 해당 소스 문서들의 `text` 길이 평균.
- 정제 결과: **UN 31k → 25k, CN 18k → 16k** (주제 문단만 추출), 전체 **371 → 354**.

**시연 포인트**
- 출력의 뜻: 정제로 17건이 사라졌고, 긴 소스의 평균 길이가 줄었다. **길이 편향**을 눈으로 확인하는 단계.

**검증 질문**: "왜 길이 편향이 분석에 위험할까?" → (긴 텍스트가 단어 빈도 등을 부풀림)

---

## 슬라이드 16 — 정제 규칙 2개 + 재현성

**한 줄 요약**: 무엇을 버릴지 규칙 2개로 명문화하고, 그 결정을 코드로 공개한다.

**설명 포인트**
- **규칙 ①**: 본문에 주제 키워드(ukraine/russia…)가 **단 한 번도 없으면** → 잘못 걸린 문서, 제거.
- **규칙 ②**: 6,000자 초과(주로 중국 브리핑) → **주제 문단 + 바로 앞 질문**만 추출.
- 철학: "분석은 **무엇을 버릴지** 정하는 데서 시작한다. 그 결정은 **기록·공개**해야 한다(`clean_corpus.py`) = 재현성, 곧 연구 신뢰성의 근거."

**검증 질문**: "정제 기준을 코드로 안 남기면 뭐가 문제일까?" → (재현 불가, 신뢰성 붕괴)

---

## 슬라이드 17 — 흔한 함정: 버리면 잃는 것

**한 줄 요약**: 정제는 공짜가 아니다 — 노이즈를 지우면 신호도 같이 지워질 수 있다.

**함정/주의**
- 중국 브리핑을 문단 단위로 자르며 **맥락(앞뒤 질문·다른 주제와의 연결)** 을 함께 잘랐다.
- 그래서 자른 기준을 코드로 남긴다. **4주차(s4) 검증**에서 잘린 부분을 다시 본다.

**검증 질문**: "우리가 자른 게 정당했을까? 무엇을 잃었을까?" → 회고(슬라이드 22)와 연결되는 열린 질문.

---

## 슬라이드 18 — Step 4: 우크라이나만 골라내기 (코드)

**한 줄 요약**: 불리언 인덱싱으로 `topic=="ukraine"` 인 157건만 추출.

**설명 포인트**
- `df["topic"]=="ukraine"` 는 **True/False 배열**. `df[조건]` 은 True인 행만 남긴다 — 이게 pandas 필터의 전부.
- `ukr.groupby("source").size()` 로 소스별 분포 확인.

**시연 포인트 / TODO**
- 빈칸 `"ukraine"` 을 채우고 실행 → **157건** 확인.

**검증 질문**: "157건 나왔어? 소스별로 몇 개씩?"

---

## 슬라이드 19 — Step 5: 침묵 지도 🤫 (개념 + 코드)

**한 줄 요약**: 외교에서 **말하지 않는 것**은 말하는 것만큼 강한 신호다.

**설명 포인트**
- `coverage_clean.csv` = 사건 × 소스 매트릭스. **0 = 침묵.**
- 각 사건 행을 돌며 문서 수가 **0인 소스**를 모은다 = 그 사건에 침묵한 소스.

**시연 포인트**
- `cov_ukr.iterrows()` 로 사건별 침묵 소스 리스트가 출력되는 걸 보여준다.

(오늘의 두 번째 봉우리 — 시간 여유를 여기에 쓴다.)

---

## 슬라이드 20 — 침묵 지도: 출력이 말하는 것

**한 줄 요약**: 한국의 **선택적 침묵** 패턴 자체가 발견이다.

**설명 포인트 (핵심 발견)**
- **한국의 선택적 침묵** — ICC 영장 · ICJ 명령 · 댐 파괴 · 에너지 시설 공격 같은 **법적·기술적 변곡점**엔 침묵.
- 반면 침공 · Bucha · 1주년 같은 **상징적·인도적 사건**엔 발언.
- 이 패턴 자체가 발견이다. "왜 한국은 이렇게 움직일까? (당사자성? 신중함?) → **s4에서 해석**."
- 이것이 6번째 차원 **Silence Map** 의 원재료.

**검증 질문**: "4개 소스가 **모두** 말한 사건 하나 찾아봐. 모두 말한 사건과 누가 침묵한 사건의 차이는?"

---

## 슬라이드 21 — Step 6: 작업 데이터 저장 (코드)

**한 줄 요약**: 우크라이나 157건을 `ukraine_working.json` 으로 저장.

**설명 포인트**
- `ukr.to_json("data/ukraine_working.json", orient="records", force_ascii=False, indent=1)`.
- 출력: `✅ 저장: data/ukraine_working.json ( 157 건 )`.
- 핵심: **이번 주 산출물이 다음 주 입력** — Session 2에서 이 파일을 spaCy로 구문 분석한다.

---

## 슬라이드 22 — 회고 질문 3개

**한 줄 요약**: 오늘 배운 걸 스스로 점검하는 3개 질문.

**검증 질문 (그대로 던진다)**
1. 4개 소스 중 **가장 긴/짧은** 소스는? 그게 만드는 분석 함정은? (길이 편향)
2. **모두 말한** 사건 vs **누가 침묵한** 사건의 차이는?
3. 중국 데이터에서 문단을 잘라낸 건 정당한가? 무엇을 잃었을 수 있나?

(학생이 직접 답하게 두고, 정답을 바로 주지 않는다.)

---

## 슬라이드 23 — 다음: Session 2 예고

**한 줄 요약**: 오늘은 **누가** 말했나, 다음엔 **어떻게** 말했나.

**설명 포인트**
- "오늘은 **누가** 말했나. 다음엔 **어떻게** — 'Russia attacked' vs 'civilians were attacked'."
- **Directness Index(직설성)** 의 탄생을 예고하며 마무리.

(클로징 슬라이드 — 한 문장으로 강하게 끊는다.)

---

## ⏱ 시간 관리 치트시트

| 구간 | 슬라이드 | 시간 | 누적 |
|---|---|---|---|
| 오프닝 + 프로젝트 지도 | 01–06 | 7분 | 0:07 |
| Step 0 환경 | 07 | 5분 | 0:12 |
| Step 1 존재 확인 | 08–10 | 10분 | 0:22 |
| Step 2 로드 | 11–13 | 10분 | 0:32 |
| Step 3 정제 | 14–17 | 15분 | 0:47 |
| Step 4 필터 | 18 | 8분 | 0:55 |
| Step 5 침묵 지도 | 19–20 | 15분 | 1:10 |
| Step 6 저장 + 회고 | 21–23 | 10분 | **1:20** |

> 10분 여유. Step 3(정제 철학, 슬라이드 14–17)과 Step 5(침묵 지도, 슬라이드 19–20)가 오늘의 두 봉우리. 여기에 버퍼를 쓴다.
